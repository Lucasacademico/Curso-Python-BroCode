# Utilizando as funções de maneira aninhada.

# Aqui seguiremos a mesma ordem do anterior, só que toda a alteração será simultânea.
print(round(abs(float(input("Digite um valor float negativo: ")))
            ))
# Perceba que a ordem seguida é de dentro pra fora.
# Uma dica (pra não se confudir), é digitar a ordem interna, cortar o código, inserir a segunda função, e colar o código, e assim sucessivamente.
